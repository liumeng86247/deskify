# 贡献指南

感谢您对 Deskify 项目的关注！我们欢迎所有形式的贡献。

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发环境设置](#开发环境设置)
- [代码规范](#代码规范)
- [提交 Issue](#提交-issue)
- [提交 Pull Request](#提交-pull-request)
- [代码审查流程](#代码审查流程)

## 行为准则

本项目遵循 [贡献者行为准则](CODE_OF_CONDUCT.md)。参与本项目即表示您同意遵守此准则。

## 如何贡献

您可以通过以下方式贡献：

- 🐛 **报告 Bug**：提交 Issue 描述问题
- 💡 **提出功能建议**：分享您的想法
- 📝 **改进文档**：完善 README、注释等
- 🔧 **提交代码**：修复 Bug 或实现新功能
- 🎨 **优化 UI/UX**：改进用户界面和体验

## 开发环境设置

### 前置要求

- Flutter SDK >= 3.0.0
- Dart SDK >= 3.0.0
- Git
- 对应平台开发工具：
  - **Windows**: Visual Studio 2022 (C++ 桌面开发)
  - **macOS**: Xcode
  - **Linux**: GTK3 开发库

### 设置步骤

1. **Fork 本仓库**

   ```bash
   # 在 GitHub 上 Fork 本仓库
   ```

2. **克隆您的 Fork**

   ```bash
   git clone https://github.com/您的用户名/deskify.git
   cd deskify
   ```

3. **添加上游仓库**

   ```bash
   git remote add upstream https://github.com/liumeng86247/deskify.git
   ```

4. **安装依赖**

   ```bash
   flutter pub get
   ```

5. **运行项目**

   ```bash
   flutter run -d windows  # 或 macos / linux
   ```

## 代码规范

### Dart/Flutter 代码规范

- 遵循 [Effective Dart](https://dart.dev/guides/language/effective-dart) 指南
- 使用 `dart format` 格式化代码
- 运行 `flutter analyze` 检查代码质量

### 命名规范

- **文件名**: 使用 `snake_case`，如 `home_screen.dart`
- **类名**: 使用 `PascalCase`，如 `HomeScreen`
- **变量/函数名**: 使用 `camelCase`，如 `currentUrl`
- **常量**: 使用 `lowerCamelCase`，如 `maxRetryCount`

### 代码风格

```dart
// ✅ 好的示例
class HomeScreen extends StatefulWidget {
  final String? cachedUrl;
  
  const HomeScreen({
    super.key,
    this.cachedUrl,
  });
  
  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

// ❌ 避免
class HomeScreen extends StatefulWidget{
  final String? cachedUrl;
  const HomeScreen({super.key,this.cachedUrl});
  @override
  State<HomeScreen> createState() => _HomeScreenState();
}
```

### 提交前检查

```bash
# 格式化代码
dart format .

# 检查代码质量
flutter analyze

# 运行测试（如果有）
flutter test
```

## 提交 Issue

### Bug 报告

提交 Bug 时，请包含以下信息：

- **问题描述**：清晰描述问题
- **复现步骤**：详细说明如何复现
- **预期行为**：应该发生什么
- **实际行为**：实际发生了什么
- **环境信息**：
  - 操作系统及版本
  - Flutter 版本 (`flutter --version`)
  - 应用版本
- **截图/日志**：如有相关截图或错误日志

### 功能建议

提出新功能时，请说明：

- **功能描述**：详细描述您想要的功能
- **使用场景**：在什么情况下会用到
- **可能的实现方案**：如果有想法，可以分享

## 提交 Pull Request

### PR 流程

1. **创建功能分支**

   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

2. **进行更改**

   - 编写代码
   - 添加/更新测试（如有）
   - 更新文档

3. **提交更改**

   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   ```

   **提交信息规范**（遵循 [Conventional Commits](https://www.conventionalcommits.org/)）：

   - `feat:` 新功能
   - `fix:` Bug 修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响功能）
   - `refactor:` 代码重构
   - `test:` 测试相关
   - `chore:` 构建/工具相关

4. **同步上游仓库**

   ```bash
   git fetch upstream
   git rebase upstream/master
   ```

5. **推送分支**

   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**

   - 在 GitHub 上创建 PR
   - 填写 PR 模板
   - 等待代码审查

### PR 检查清单

提交 PR 前，请确认：

- [ ] 代码遵循项目规范
- [ ] 已运行 `dart format` 和 `flutter analyze`
- [ ] 已更新相关文档
- [ ] 提交信息清晰明确
- [ ] 已测试功能正常工作
- [ ] 没有引入新的警告或错误

## 代码审查流程

1. **自动检查**：GitHub Actions 会运行代码检查
2. **人工审查**：维护者会审查代码
3. **反馈与修改**：根据反馈进行修改
4. **合并**：审查通过后合并到主分支

### 审查要点

- 代码质量和规范性
- 功能完整性和正确性
- 测试覆盖（如有）
- 文档完整性
- 性能影响

## 许可证

通过贡献代码，您同意您的贡献将在 [木兰宽松许可证，第2版](LICENSE) 下授权。

## 获取帮助

如有问题，可以通过以下方式获取帮助：

- 提交 Issue
- 查看 [README.md](README.md)
- 访问项目官网：https://deskify.jiahetng.com

再次感谢您的贡献！🎉

